<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Athlete Confirmation Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>

<body>
    <h1>Athlete Confirmation Page</h1>
    
    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>AKA</th>
                <th>Weight</th>
                <th>Height</th>
                <th>Team</th>
                <th>Confirmation</th>
            </tr>
        </thead>
        
        <tbody>
            <% if (athletesData.length == 0) { %>
                <tr>
                    <th scope="row">ไม่มีข้อมูลนักกีฬา</th>
                </tr>
    
            <% }else { 
                athletesData.forEach(athlete => { %>
    
                    <tr>
                        <td>
                            <%= athlete.user.fname %>
                            <%= athlete.user.lname %>
                        </td>
                        <td>
                            <%= athlete.nickname %>
                        </td>
                        <td>
                            <%= athlete.weight %>
                        </td>
                        <td>
                            <%= athlete.height %>
                        </td>
                        <td>
                            <%= athlete.team.teamname %>
                        </td>
                        
                        <td>
                            <!-- Create a form or button to confirm the athlete -->
                            <a class="btn btn-warning" href="/athletes/info/<%=athlete._id%>"class="mr-2">info</a>
                            <!-- <form method="POST" action="confirmAthlete/<%= athlete._id %>">
                                <button type="submit">Confirm</button>
                            </form> -->
                            
                        </td>
                    </tr>
                <% }); 
            } %>
        </tbody>
      </table>
</body>

</html>
